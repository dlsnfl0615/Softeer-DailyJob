# 오늘 할 것
1. S3 putobject 권한 풀어달라고 요청
2. 웹소켓 개념 공부
3. presigned url 테스트
4. upload 도메인과 dreami 도메인에 swagger와 각종 규약 어노테이션 붙이기
5. issue 46 완성
6. api 테스트 코드에 어노테이션 붙여서 빌드할 때 제외하게 수정

# 오늘 한 것
## @PathVariable
```java
@RestController
@RequestMapping("/users")
public class UserController {

    @GetMapping("/{userId}")
    public String getUser(@PathVariable("userId") Long userId) {
        return "요청한 유저 ID: " + userId;
    }
}
```
/users/{userId} 경로의 {userId} 부분(중괄호로 감싼 부분)이 실제 요청 시 URL에 들어오는 값(예: /users/42)과 매칭되어 userId 파라미터에 42가 들어감
매개변수 변수명을 경로 변수명과 일치시키면 @PathVariable에 이름 안 써도 됨

## MFA 인증 안 받고 Access Key 사용해서 오류
### 1차 시도
일반 IAM 유저 자격증명으로 만든 presigned url이어서 scp에서 차단함

### 2차 시도
MFA로 받은 임시 자격 증명의 키들을 넣어서 요청하니까 성공
근데 만료 기간이 설정되어 있어서 자동화가 필요함

### 3차 시도
ec2 인스턴스에 IAM role 만들어서 s3에 대한 PutObject와 GetObject 권한 허용

## DB에 사진 URL을 뭘로 저장해야 하는가
### 민감 정보
신분증 사진 같은거는 key만 저장해두고 관리자가 신분증을 확인해야 하는 시점에 요청

### 자주 조회해야 하는 사진
CDN(CloudFront) + Public/서명된 URL

## 매칭 큐 시스템
### Action의 정의
- 드리미가 대기중이다
- 부르미가 주문을 등록했다
- 부르미 요청을 재주문한다

### BlockingQueue<Action> queue
Action을 큐에 집어넣음.
이거 쓰는 이유는 thread safe를 위해서. 한 thread에 있는 큐에 Action들 넣는 것.
queue에서 한 Action이 수행 완료되면 다음 상태를 queue에 집어넣음.
기다리고 있는 드리미나 하나의 주문에 대해 동시에 뿌린 제안 묶음 등은 map(방)으로 관리함.

### 드리미들에게 주문 뿌려주는 방식
일정한 반경내에 있는 드리미들에게 뿌려줌.
미리 설정해둔 인원수, 예를 들어 10명의 드리미들에게 뿌림.
이 10명이 모두 매칭 거절하면 해당 부르미의 주문을 다시 재주문 Action으로 queue에 집어넣음.
미리 설정한 인원수에게만 뿌리기 때문에 반경내의 새로운 드리미를 찾아서 계속 보여줄 필요가 없음.

### 그럼 WebSocket vs SSE?
새로운 드리미를 계속해서 가져오는 것이 아니라 일정한 인원수의 드리미들에게 뿌리고, 이들이 모두 거절한다면 새로운 요청을 큐에 집어넣는 방식이기 때문에 완벽한 실시간성이 필요없음.

# 내일 할 것(오늘 밤에 할 것)
1. 동료 피드백
2. 자가 피드백
3. 시연 html 만들어서 사진 잘 보내는지 확인
4. 자주 사용하는 사진은 어디에 저장하는 건지 더 자세히 알아보기
