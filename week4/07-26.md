## 추가 학습
### curl로 테스트하기
- 도로명주소 잘 들어왔다는 가정하에 위도 경도로 반환
  ```bash
  curl -X POST http://localhost:8080/api/address/places \
  -H "Content-Type: application/json" \
  -d '{"start": "서울 강남구 강남대로62길 23", "destination": "서울 강남구 학동로 171"}'
  ```
- 
